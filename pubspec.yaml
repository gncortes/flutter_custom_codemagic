name: custom_cicd
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  sqflite: ^2.4.1
  path: ^1.9.0
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.4
  build_runner: ^2.4.13
  flutter_lints: ^4.0.0
  sqflite_common_ffi: ^2.3.4

flutter:

  uses-material-design: true

